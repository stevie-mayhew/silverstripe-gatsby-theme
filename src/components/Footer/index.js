import React from 'react';
import { Layout } from 'antd';

const { Footer: LayoutFooter } = Layout;
const Footer = () => (
  <LayoutFooter>
    Footer
  </LayoutFooter>
);


Footer.propTypes = {
};

export default Footer;
